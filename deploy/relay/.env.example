RELAY_DOMAIN=relay.example.com
ACME_EMAIL=you@example.com

# Optional host ports and listen address; the relay backend is never published.
# RELAY_BIND_ADDRESS=0.0.0.0
# RELAY_HTTP_PORT=80
# RELAY_HTTPS_PORT=443
