# code3d 工具架构

工具的职责是把交互转换成编辑意图，而不是直接修改模型、Monaco 或 AST。

源码修改取消旧构建时，立即拒绝旧请求并通过共享信号请求 Worker 退出。内核只在
完整操作之间检查信号，已有结果随求值的 `finally` 保留为工作集，历史仍按 LRU
管理。旧构建收尾后只执行最新排队版本；取消结果不生成可导出快照或更新视图。
LRU 使用默认 2 GiB 预算，统计内核已分配块与缓存 JavaScript 数据估算；不再限制
历史条数。完整工作集受保护，预算不是页面总内存硬上限。WASM 容量不随释放缩小，
淘汰依据原生分配器的存活块占用，而非 `wasmMemory.buffer.byteLength`。
取消后 5 秒仍未退出则强制终止并重建 Worker，此时内存缓存丢失；持续编辑不延长该时限。
关闭项目仍立即释放 Worker。准备阶段先完成依赖失效处理，再响应取消。

App 和 agent 编译 Worker 共用同源 OPFS 持久缓存（#79）。编译前异步打开两份
日志文件，操作期间同步按需读写，正常结束、抛错和合作取消均 flush；强制终止只
保留此前已持久化的部分。追加记录带完整操作签名和 CRC 校验，新代整理文件在
完整 flush 后发布，再回收旧文件。存储失败时继续使用内存，下次编译重试。
同源 Web Lock 覆盖打开、编译和关闭，等待可取消，空闲 Worker 不持有文件锁。
同源编译仍串行取得持久日志所有权；一次编译内部的计算池共用主 Worker 的结果缓存。

快照阶段（#80）先从真实几何、选择和组合位姿收集 bounds/mesh 查询，按 artifact 键
去重并检查两级缓存，再按输入几何归并未命中任务。默认最多 4 个计算 Worker，按
可用 CPU 缩减；单 Worker 环境使用相同的查询协议。动态关系求解和作者代码仍同步
执行。首次按拓扑规模与查询量预估耗时，后续结合实测成本，长批次先派发，空闲 Worker
领取下一批；相同几何只在一个批次内恢复一次。

主 Worker 是唯一 LRU 和 OPFS owner。计算 Worker 只保留当前批次的独立原生输入，
逐项返回自有网格缓冲和 bounds，主 Worker 立即入缓存。没有每个 Worker 独占的
2 GiB 历史预算：子内核分配块、在途输入和调度元数据并入主池内存账目；预算压力下
减少新批次的并发入场，完整工作集仍允许超出软预算。WASM 保留容量和运行时代码
仍不等同于缓存存活量；报告的峰值是在操作/传输边界观察到的核算峰值，不声称覆盖
单个原生调用内部的瞬时峰值。

网格在本地和远端都使用同一二进制几何输入，避免 BinTools 对旋转平面方向的
ULP 归一化改变 Delaunay 对角线选择，并隔离 mesh 对原始输入的修改。bounds 使用
解析几何，不依赖已有网格。原生变换统一显式释放 gp_Trsf、构造器、返回句柄和
中间几何；缩放查询借用输入，不能调用会销毁 receiver 的便捷变换方法。
预编译的项目运行时模块与实际 WASM 被用于所有计算
Worker，依赖变化或项目释放会一并销毁计算池。

取消在查询边界退出，先保留刚完成的结果；两秒仍无响应的计算 Worker 被终止。
崩溃或任务超时仅重试未完成查询一次，失败不会留下永久等待的缓存键。主编译器仍
等待收尾后只运行最新 revision，关闭项目会释放整个池。统计包含批次/查询数、
重试、编码、恢复、计算、初始化、派发和排队耗时，及输入字节和边界内存峰值。

磁盘物理文件预算为 min(1 GiB, 浏览器总配额的 10%)，一半留给整理副本，不预分配。
索引只保留键、位置和 LRU 顺序，不预载历史几何。实际依赖文件（含 Core、Replicad
及 codec）和内核/求解器 WASM 内容的 SHA-256 隔离命名空间；临时 Blob URL 不参与。
几何使用 BinTools 二进制 BREP，拓扑、local bounds、reference basis 和 mesh 一并保存。
保存的变换、ID 和 mesh 精确恢复；BinTools 重建方向轴时的归一化可能让重新查询的
原生法向、面积等产生浮点末位差异，不能把所有原生查询当作逐位一致的承诺。
普通 Node 作者入口仍使用内存缓存；CLI/agent 经浏览器编译链路获得持久复用。

```text
selection + gesture
        ↓
       tool
        ↓
      intent
        ↓
     resolver
        ↓
 edit plan + preview
        ↓
 source transaction
        ↓
    compile model
```

## 不变量

- 源文件是模型持久化状态的唯一来源。
- preview 可以临时改变视图，但不能成为隐藏的模型状态。
- 工具不直接依赖 Monaco、OpenCascade 或 Three.js。
- 每个 source ref 都包含项目文件路径和文件内 offset；offset 不能脱离文件解释。模型完成编译后，editor 在下一版模型到达前持续追踪工具所用 source ref 的最新范围。
- 所有提交都带项目 revision 和 expected text，跨文件 edit plan 也必须原子应用。
- 无法唯一解析的意图必须提供候选方案，不能静默猜测。
- Caret 选择源码 occurrence 及其 operation role。点击当前 focus 集合中的
  occurrence 只细化 runtime instance，不改变源码 context；点击 dim 的
  operation peer 则导航到该输入的 source target，并将它切换为 focus。
  decoration 不参与拾取。

## 核心层次

### Tool

处理 pointer、keyboard 或 gizmo 输入，并产生 `ToolIntent`。同一个意图可以来自不同 UI。

### Intent

描述用户想完成什么，而不是具体文本改动。目前定义：

- `parameter.set`：修改已有上游参数。
- `expression.replace`：用结构化 Expression Draft 替换表达式。
- `relation.offset`：调整选中对象的位置关系；必要时在约束表达式上构造 `.offset()`。

### Resolver

结合 provenance、selection scope 和源码 anchor，把 intent 解析为一个或多个 edit plan。参数修改优先使用已有声明；结构修改可以插入调用、提取变量或生成局部表达式。

### Edit plan

包含：

- 工具和 intent。
- 开始交互时的源码版本。
- 可供 UI 展示的修改摘要。
- 临时 preview 描述。
- 带 expected text 的源码编辑集合。

### Source transaction

提交时再次检查项目 revision 和所有文件中的 expected text，然后按文件作为一个逻辑事务进入各自 Monaco undo stack。失败时不应用部分结果。
事务保留用户原有 caret 和由它决定的渲染 scope；工具不会为了展示写入位置而移动 selection。提交成功后，UI 使用 edits 的 expected text 与提交后源码，按文件计算真实行级 diff。独立 popover 默认只显示一行 `model.ts +3 −1`，不重复操作意图；修改一行计为 +1 −1，同一行多处修改不重复计数，整段 sketch 参数替换不等于整段改变。点击摘要展开带少量上下文的增删行；只有用户点击差异行时，编辑器才定位并聚焦当前源码（删除行定位到其后的存活位置）。每次成功事务更新同一提示，不按预览帧堆叠，无变化则不展示；悬停或焦点在提示内时不自动关闭。

GUI 不维护第二套历史。编辑器界面没有焦点时，`Ctrl/Cmd+Z`、
`Ctrl/Cmd+Shift+Z` 和 `Ctrl/Cmd+Y` 直接作用于当前活动文件的 Monaco undo
stack，且不夺走 viewport 或面板焦点；Monaco 及其内部输入有焦点时仍由编辑器原生处理。
任何后续源码变化都会关闭已经过期的 source-update popover。

## Tool session

1. `begin` 固定工具和交互 scope；source anchor 由 editor 跨 revision 追踪。
2. `preview` 可以被连续调用，只产生临时视图。
3. `commit` 针对最新 source anchor 和 editor revision 重新解析 intent，再原子写入源码。
4. `cancel` 清除 preview，不产生源码修改。

模型重新编译后，tool session 即结束；后续工具必须基于新的 provenance 开始。
源码事务提交后，GUI 将 preview 提升为当前 revision 的 optimistic 状态。编译期间仍可继续交互；新的交互立即丢弃正在运行或等待中的旧 revision 编译，新的源码事务产生后只编译最新 revision。对应模型完成后，以原 selection 和 scope 替换 optimistic 状态。

## Completion-derived preview

Monaco 原生 completion list 中当前聚焦的普通文本候选会生成一个非持久化 project
snapshot：只在 snapshot 中应用 Monaco 已计算的主 edit 和 additional edits，并把补全
后的虚拟 caret 位置用于选择编译结果。这个 snapshot 使用正常 compiler/runtime 路径，
所以 viewport 展示的是“接受该补全后”的模型语义，而不是仅替换标签的视觉猜测。

真实 Monaco model、caret、undo history、文件系统和 source revision 始终不变。补全候选
切换时丢弃旧 speculative compile；候选关闭后恢复真实 module，并重新编译实际源码。
speculative module 只属于 viewport transient preview，不替换工具使用的已接受 module，
也不允许 viewport picking 或 source write-back。具名 element 候选在 speculative compile
完成前先复用已接受 module 显示即时高亮。

speculative compile 等待期间，viewport 边缘显示显著但不阻挡交互的 progress 状态；
候选结果就绪、失败、关闭或被新候选取代时立即移除。

## 编辑器布局

文件树和代码区独立保存像素宽度偏好，两个分隔条共用手势、取消与键盘处理。
文件树使用 Pierre 的 vanilla FileTree，直接枚举 ProjectFileSystem 的文件和空目录，
不依赖 Monaco 已加载的文档列表；`.code3d`、`.git`、`node_modules` 目录保持隐藏。
仅将会话中待保存的文件路径补入目录清单；缓存文档不会补回已从外部删除的文件。
编辑器按需加载 UTF-8 文本，源文件仍作为编译入口，其他文本只编辑不执行模型。
目录操作与用户保存、agent apply 共用项目会话的串行队列；先检查整批路径冲突，
每个磁盘操作成功后同步对应文档、tab 和 agent 路径，失败后从实际目录重建树。
不将 Pierre 的同步重命名/拖动回调当成异步事务。复制保留二进制字节与空目录，
删除最后一个文件后允许项目为空，已有 manifest 的空项目不会再次写入初始文件。
树的选择与活动文档分别维护，拖动选择不打开文档；关闭最后一个 tab 时清除树选择，
确保重新点击可以打开。定位深层活动文件时先展开祖先目录，再滚动到对应行。
内部 Shadow DOM 通过主题变量和局部滚动条规则沿用 App 外观。
侧栏采用 24px 树行与操作按钮、14px 图标、8px 外侧留白；搜索与重命名输入框
明确设置盒模型和边框，普通悬停、选择、键盘焦点使用同一组编辑器界面颜色。
菜单按新建、编辑、删除分组并显示快捷键；窄侧栏收起标题，极窄时收起搜索按钮，
保留搜索输入框与快捷键。新建弹窗的标题、路径、名称和右对齐操作按钮统一排版。
文件树默认 184px，常规范围 140–420px；窗口不足时进一步约束实际宽度，为代码区
保留 280px、左右布局的预览区保留 460px。调整文件树尽量保持代码区偏好，临时
窗口约束不覆盖已保存宽度。文件树分隔条随文件树折叠隐藏，在上下布局中仍可使用。

分栏取消恢复的是本次拖动前对应面板的用户宽度偏好，而非默认宽度。代码分隔条进入
上下布局时保留最后一次左右布局的文件树/边框宽度，取消仍立即同步 CSS 与 ARIA，
不等待下一次 ResizeObserver；
快速缩窄再恢复窗口时即使通知被合并，也不能残留拖动中的视觉宽度或写入新偏好。

## Model diagnostics

模型编译和执行错误以结构化 diagnostic 穿过 worker 边界，保留 kind、summary、
details 和可选的 `SourceRef`。最内层的源码求值边界负责为普通 runtime/kernel error
补充位置；已有精确位置的错误经过外层调用时不会被覆盖。参数校验、关系求解、Boolean
求值及 snapshot 生成因此都归属到请求该次求值的源码表达式，而不是从错误文本或编译后
stack 反推位置。

带 `SourceRef` 的 diagnostic 由 Monaco marker 直接标在源代码上，hover 使用编辑器原生
诊断界面展示详细信息；每次编译替换为当前错误及警告集合。编辑期间保留上一版 marker，直到新的编译结果被
接受，避免异步编译造成错误状态闪烁。只有项目、worker、超时等无法可靠归属源码的错误
才使用全局 error bar。

草图模式下，viewport 的 Model error 状态和详细错误卡片共用同一筛选：只有失败调用的
execution ID 属于当前草图或其上游的定义求值时才显示。失败的下游派生不归属其 receiver，
同一工厂源码的不同运行实例也不混淆。语法、模块和无归属错误只走编辑器/全局诊断；
无关草图、3D 模型及其局部预览错误不进入草图 viewport。非草图模式保留原有 3D
模型依赖关系和局部预览的诊断策略。

成功求解的草图若作者几何与显示结果超出局部图元的几何容差，返回非阻塞 warning：
代码处标黄，当前草图/上游的提示卡解释不同步，状态仍为 Ready，不自动回写。
诊断的通用 `actions` 保存标签和可序列化 `ToolIntent`，展示层不识别具体修复算法；
执行仍走 ToolEngine、编译版本校验、源码 expectedText 校验、差异反馈和一次撤销。
草图的 Fix 入口为右对齐的下划线文字，无按钮底色和边框；保留键盘焦点与禁用语义。
它只同步本层变化的字面量坐标/半径，保留约束表达式、实体 ID 和上游。
若完整修复需要替换几何表达式，或同段源码有多个运行实例，只警告并说明原因。
动态数组仍可警告，但不伪装成可回写源码；上游修复要求先打开所属草图。

## Viewport decoration

临时 3D 辅助显示使用通用的 `ViewportDecoration`。它是 renderer-neutral 的
discriminated union：`mesh` decoration 携带派生网格和 transform，`edges` 可显示
完整或按稳定 ID 过滤的边线，`surface` decoration 把网格子集附着到指定模型
occurrence，`anchor` decoration 携带模型 node、point/line/face/frame 类型和局部
frame。viewport 只把这些数据渲染为辅助几何，并按 owner 设置或清除 decoration
layer，不理解具体工具、建模操作或元素名称的语义。

所有 decoration 都必须声明 `nodeId`，几何和 transform 属于该模型的局部坐标；
viewport 按当前可见 occurrence 放置，不把缺少归属的 transform 默认为世界坐标。
布尔 region 的 `frameNodeId` 与用于区分操作数的 `inputNodeId` 分别记录；其几何
属于主输入，不能借用可能因形状变化而重新求解的输出位姿。圆角/倒角对比使用
selection 的输入到输出变换，并附着到结果实例。

与源码 scope 相关的辅助显示实现为 `SourceDecorationProvider`。provider 读取 runtime operation metadata 并返回 decoration；工具开发者可以注册新的 provider，无需修改 viewport 的选择或渲染主路径。交互中的工具则返回 `viewport-decorations` preview，由 host 按 owner 应用和清理对应 layer，工具本身仍不直接调用 viewport。

需要精确派生几何时由 kernel/runtime 产生 operation region，provider 只决定何时、
以何种 appearance 展示。当前 Boolean provider 用 `mesh` decoration 强调 `cut`
的切除体积，以及 `union` 的重叠体积或仅接触时的 B-Rep section；named-element
provider 用 `anchor` 标出元素 frame，并为 face 元素按其平面和法向选择真实 B-Rep
face group，再用 `surface` 显示实体面及其边界。fillet/chamfer provider 在结果源码
上下文中用弱化 `mesh` 保留操作前形状，并用 `edges` 强调被修改的原边。这些都不是
viewport 中的专用操作语义分支。

Provider 可通过 `previewBehavior` 声明工具预览期间的显示策略。当前 Boolean provider 使用 `hide`：参数或关系工具开始移动后隐藏旧 region，pointer move 不触发模型或 region 重算；取消时恢复已编译 region，提交后等待正常源码编译产生新的精确 region。这个策略只影响对应 provider 的 layer，不影响实体、gizmo 或其他工具 decoration。

## 当前接入的工具

`@code3d.param` 可直接放在公开的 callable 变量上，例如
`export const sleeve = definePrimitive((radius: number, y = 4) => ...)`。
tooling 从公开声明读取注释，并从 TypeScript 实际调用签名解析参数名称、顺序、
可选性和数组类型，包括泛型工厂返回的具名参数 tuple。普通别名、import/re-export
和已发布的 `.d.ts` 使用同一规则，不要求函数 wrapper 或专用 metadata 选项。
具名函数与方法的重载仍优先使用匹配签名上的注释。primitive factory 不额外提供
`@code3d.arguments` 入口；独立预览使用普通示例调用。

数值参数的 `@code3d.param` 可用 `default: 60` 描述省略实参时的面板
placeholder；源码与已发布 `.d.ts` 使用同一静态解析路径。默认值接受有限数值
字面量，count 必须为整数，且满足参数 constraints；静态校验与面板编辑共用数值
规则。函数参数初始化器只决定实际运行行为，不读取其默认值，也不把 annotation
注入非交互运行时；作者负责保持描述与实现一致。

必填与可选参数都可描述默认值；`default` 不改变签名的可选性。内置标量尺寸图元
用必填公开重载保留 TypeScript 缺参诊断，实现在省略或 undefined 时提供运行时默认值，
普通运行与 App 预览一致。显式非法值仍按原约束报错。源码已选中的工具调用即使
没有生成几何也应退出初始空视图，保持参数工具可见。见 [#92](https://github.com/vilicvane/code3d/issues/92)。

默认 placeholder 仅用于源码确实省略的实参。已有实参（包括显式 undefined、
求值为 undefined 的表达式和求值失败）维持原展示。所有连续省略参数均可展示
各自默认值，但仅下一位置可以补写。参数是否省略与能否写回分别记录；展开实参
及后续位置无法静态映射时标记为 unknown，不展示默认值或提供位置编辑。聚焦、离开和未输入时提交不修改源码；主动
输入同一数值也会写成显式实参。清空仍属无效输入，Undo 或源码删除实参可恢复
省略状态，不新增恢复默认按钮。实现背景见 [#29](https://github.com/vilicvane/code3d/issues/29)。

viewport 平移 gizmo 在能够唯一追溯参数时产生 `parameter.set`，否则针对已有关系产生
`relation.offset`。源码中的 `fillet(radius)`、`fillet(radius, edgeIds)` 和对应的
`chamfer` 调用把整个参数区域投影为 edge-selection source target。`edge-operation.set`
可以写入非空的显式 ID 数组，也可以删除第二参数以恢复单参数的全部边语义；每次修改
立即进入共享源码事务，同一轮工具交互合并为一个 Monaco undo 历史项。

上下文工具面板的展示由选中的源码位置决定，不由 `Esc` 关闭；离开对应调用才结束
该面板的交互。`Esc` 优先取消尚未松手的 viewport 拖动，保留面板与代码位置；
未拖动时不结束面板的参数编辑或拓扑选择，也不撤销已经提交的源码修改。

compiler 为每个实际进入的 call site execution 统一记录完成或失败状态、单调触达顺序、
已求值的 operation inputs、实际参数和参数 provenance。source target 的同一 context 内
默认采用最近触达的一次 execution；工具只投影自己需要的已捕获上下文，不维护单独的
失败生命周期。fillet/chamfer 因而可在操作失败时使用已求值的 receiver、实际边数组和
尺寸参数继续工作；不可用于 receiver 的失效 ID 不进入 GUI 选择，下一次写回即可清除。

receiver 与参数中的模型值按实际求值自动记录，不按建模 API 名称登记。数组和选项对象
字面量保留成员各自的源码范围；变量和展开表达式也可携带模型集合。输入角色由运行时
operation 的输入记录关联，因此导入别名、namespace 调用和计算属性方法共用同一路径。
没有对应 operation 或调用失败时，已取得的输入仍可作为模型值预览；同一源码位置的多次
执行保留各自的输入，采用最近触达的值。追踪不主动读取 getter。空间手柄仍由 operation
metadata 和参数 provenance 定义轴、pivot 与编辑语义。

带工具注释的调用即使在产生模型值之前失败，也会发布独立的 tool source target。有效调用
严格采用 TypeScript 实际匹配的重载；没有重载可匹配时，优先保留 TypeScript 的带注释
恢复候选，否则按声明顺序选择第一个带注释的候选。候选恢复只接受落在当前 callee 或其
直接参数上的调用签名 diagnostic，不能因外层调用把当前表达式标成错误参数而误判。缺失
参数在面板中显示为空，当前调用末尾能够合法追加的下一个参数可以直接填写并写回源码，
后续参数随调用补全依次解锁。已有数值参数无法唯一解析出整个字面量或上游 target 时，
不论追踪到了零个、一个内联系数还是多个候选，均把运行时求值结果作为空 input 的
placeholder；填写数字会替换整个参数表达式，填写与 placeholder 相同的值也会替换。
它与尚未填写的参数保持区分。唯一可编辑的上游参数仍显示有效值并反向修改其来源。
面板参数的绑定、显示和输入 intent 集中在同一个模块；它与 gizmo 共用源码 provenance
候选规则，禁止各自根据候选数量引入不同的表达式分类。

参数 schema 是按能力扩展的 tagged union。数值 `kind` 提供标量控件；`vertex`、`edge`
和 `surface` 提供 topology selector，其单选或多选能力从声明参数是否为数组推导，而不是
再写一份交互配置。几何模型的 `.vertex(id)`、`.edge(id)` 和 `.surface(id)` 使用单选，
`.vertices(ids?)`、`.edges(ids?)` 和 `.surfaces(ids?)` 使用多选；两者共享同一个 provider。
compiler 在调用进入时记录 receiver 和已求值参数，所以参数缺失或引用已退休拓扑而失败时
仍可显示 receiver 并修复；viewport pick 产生通用 `argument.set` intent，立即写回同一个
源码事务。复数引用允许空数组，并在省略参数时返回全部当前稳定拓扑引用；fillet/chamfer
的省略参数全选语义仍由专用 provider 处理。

`shell(thickness, removedSurfaceIds?)` 复用通用 surface 多选与数值参数工具：
正厚度向内，负厚度向外；省略或空数组表示没有开口的封闭空腔。
通用选择器按运行时解析后的选择显示摘要和高亮，不把省略参数硬编码为全选。
操作 snapshot 的 selections 支持 vertex、edge 与 surface，并记录输入几何到输出局部坐标的变换；抽壳选择指向操作输入，
视图显示操作输出，失败时仍可修改输入面与厚度。Close all openings 删除面数组，
恢复封闭空腔；全部选中面会报错，因为必须保留壳壁。见 [#37](https://github.com/vilicvane/code3d/issues/37)。

工具在已有表达式边界插入相邻参数时，不扩张该表达式的源码范围；零长度插入槽
则跟踪刚写入的文本，以便下一次工具操作直接替换。用户输入允许在词法边界扩展
范围。这样选面后立即修改厚度无需等待编译，也不会将面数组误当作数字的一部分。

数值参数的源码 provenance 与工具签名共用一个 TypeScript semantic program。调用参数中的
标识符、具体对象属性、字面量类型的计算属性和对象解构会沿唯一 symbol definition 继续
追溯，也可以穿过 import 和 re-export，直到静态数值 initializer。任一步存在多个定义、
缺少可追溯 initializer，或 receiver 的运行时对象不唯一时，该链路不产生可编辑 target。
这套索引只覆盖带工具注释的数值参数表达式；panel 与 viewport 对同一表达式都优先采用
表达式之外的上游 target，避免把比例因子等内联字面量误当成主要参数。

平移 gizmo 目前遵守以下解析规则：

- 当前源码 occurrence 必须是提供相对位置语义的 operation input，或具体的
  constraint source site；变量声明和 operation output 即使对应对象带有 relation，
  也不显示 gizmo。
- 优先修改选中对象关系约束中能够唯一追溯的位置参数。
- 参数归属到具体 API 调用；连续变换只编辑当前最外层调用，不重复追加操作。
- 同一参数表达式同时包含上游变量和字面量时，优先修改上游变量。
- 没有可用上游参数时，只有整个参数是数值字面量才直接改值；表达式内部的字面量（例如 `i * 8` 中的 `8`）不作为拖动目标。
- 参数在整个模型中的用途必须都是同一位置轴，避免 preview 遗漏尺寸等副作用。
- 没有唯一安全参数时，优先在最外层 `.offset()` 的对应参数表达式上加减增量，保留原表达式与运算优先级；连续拖动合并末尾数值增量，归零时移除增量。
- 只有最外层不是可逐轴编辑的 `.offset()`（例如尚未添加 offset 或参数为 spread）时才追加一次字面量 `.offset(dx, dy, dz)`；后续拖动复用它，不反复嵌套，也不猜测内部组件应如何移动。
- 拖动值遵守参数的 `min`、`max` 和 `step`；没有 `step` 时使用按参数 kind 推断的步长。
- preview 会更新所有使用该参数的 occurrence，并标出选中对象以外的受影响对象。
- 松手才写入源文件；`Esc` 优先清除当前拖动 preview，恢复拖动前的位置，不产生源码修改，之后松手也不提交。取消拖动不关闭上下文工具面板。

源码 context 不提供相对位置语义、对象没有位置关系或关系接收者无法稳定定位时隐藏
gizmo。后续 choice UI 可以进一步提供“调整当前关系”或“修改内部组件关系”等不同
scope 的 edit plan。

## 模型原点与旋转

`originOffset`、`originVertex`、`originCenter` 和 `rotate` 在 operation snapshot 中记录
结果模型坐标下的局部零点和本次操作向量。模型 snapshot 的原点恒为零，tooling
统一这些数据、参数 provenance 和草图快照，并从同一依赖图安装 OpenCascade 和草图约束求解器；
viewport 不从包围盒推断模型旋转中心。

拓扑引用同时报告约束所属模型、几何来源、源拓扑身份和几何到所属模型的变换。
值预览用来源网格和该变换高亮原拓扑；子拓扑选择在同一坐标关系下只提供所属面/边内的 ID。
计算锚点直接报告坐标系，预览无需从名字反查模型的固定具名元素。`expose` 的命名成员及嵌套成员
保留拓扑元数据，补全预览遵循同样的装配坐标语义。见 [#27](https://github.com/vilicvane/code3d/issues/27)。

`originCenter()` 取主体建立时的局部包围盒中心随模型变换后的位置，与 `.center`
使用同一锚点。无参数操作通过 operation metadata 接入原点标记和手柄，不需要虚构参数。

`originVertex` 复用 topology selection provider：候选 ID 来自操作输入，候选几何通过
selection 的 transform 转换到操作输出的局部坐标，再应用当前 occurrence 位姿。
候选绘制、悬停、拾取与选择高亮共用该变换，并随 occurrence 预览一起更新；重新选择
原点后候选点仍贴合结果模型。背景网格位于 XZ 零平面，不用几何偏移规避深度冲突。
原点和偏移显示平移箭头，`rotate` 显示角度参数对应的旋转环；固定
X/Y/Z 顺序意味着 X 环包含后续 Y/Z 的方向，Y 环包含后续 Z 的方向。

`model.spatial` intent 通过通用源码事务修改唯一安全的参数，或保留当前参数表达式
并折叠末尾数值增量。拖动顶点或中心原点时生成或复用 `originOffset`。preview 保存临时
刚体变换和原点标记；旋转预览使用新旧完整旋转的差，松手写源码，Esc 清除预览。
原点拖动固定开始时的 snapshot，候选标记显示总偏移，几何保持在该输入参考系；
提交时将几何重表达为 p-d，标记回到结果模型零点，取消恢复起始状态。
原点标记的 `frame: 'operation'` 表明其位置属于空间操作的参考系，排除 occurrence
上为预览几何而叠加的变换；普通几何锚点则随几何变换。提交到重新编译之间以及
这一期间继续拖动时，也使用同一规则，不能让原点标记再次减去几何偏移。
已有 relation offset 工具与这些操作共用轴手柄和会话机制。

坐标参数使用解构数组声明，例如 `pivot([x, y, z]: Vec3)`，各分量通过
`@code3d.param x/y/z` 标注。signature 保存参数到调用实参及数组分量的路径，
源码范围、运行值和数值 provenance 按同一路径解释；不把整个数组作为一个数值。
字面量数组及其括号/类型断言可逐分量编辑，无法静态定位的数组表达式保留运行值，
不猜测源码写入位置。

## 表达式构造

工具使用 `ExpressionDraft` 描述 number、string、identifier、array、binary、call 和 member，而不是直接提交任意字符串。例如边选择工具产生：

```text
expression.replace(
  target = source ref of [2, 5],
  expression = array([number(3), number(7)])
)
```

edge 工具在 caret 落入 fillet/chamfer 的整个参数区域时解释 viewport 点击。
主实体是当前边集合已经应用后的操作结果，操作输入的全部原边同时保留在原位置作为
独立的可选择图层；数组中的 ID 预先高亮，边以模型内稳定数字 ID 显示为 `E3`、`E7`。
每次 toggle 都立即通过共享源码事务写回普通数字数组（例如 `[3, 7]`），并后台重编译；
选中集合按 ID 排序，只有集合发生变化时才提交。取消最后一个显式选择会删除第二参数，
恢复单参数的全部边语义，不保存空数组。离开调用时结束选择；`Esc` 不关闭面板或撤销修改。

Resolver 再生成带 source anchor 的编辑计划。当前 prototype 已具备基础表达式替换；
新增 fillet/chamfer 调用仍由用户手写，不提供 GUI 插入入口。多方案 UI 尚未实现。

## 草图编辑

草图画布和 3D viewport 一样铺满右侧区域，不再有内嵌面板边框和留白。
工具栏右上对齐，以纯图标按选择、绘制、修改、视图排列；矩形与中心矩形共享一个记忆
最近构造方式的入口，箭头展开变体菜单。名称和操作说明使用原生 hover 提示，
按钮保留可访问名称与 pressed 状态。当前工具强调背景，Snap/约束显示开关只强调图标。
工具栏使用一个 Tab 停靠点，左右方向键及 Home/End 导航，跳过不可编辑工具；
与画布、数字输入各自的键盘作用域分开。窄 viewport 中整条工具栏移到左上状态下方，
不拆散工具组，也不遮住编译提示。变体菜单支持上下键、Home/End、Enter、Esc 和点外关闭。
右键拖动平移，与 3D viewport 一致；中键及空格加左键也可平移，平移不取消绘制草稿。
移除左下常驻操作说明，仅保留错误和只读原因；绘制数字输入仍浮于画布上。
草图显示期间隐藏 3D 面板和 gizmo，保留紧凑源码更新提示。重编译失败且光标仍在原选中源码
范围内时，保留最后成功的草图并明确标为只读旧结果；这只是显示快照，不形成持久状态。
跟踪的源码范围随编辑移动，离开该范围或文件则退出旧草图，成功编译后恢复正常编辑。

`sketch(entries, {constraints})` 将当前几何数据与约束分开，约束不持久化 ID；
`sketch()` 和 `base.derive()` 允许省略空的本层数组。首次成功绘制时，数组、几何与
所需约束在同一源码事务中生成；取消不预先改写调用，撤销恢复原始调用和注释。
省略参数的可编辑追踪只认可公共构造调用的解析签名，不放开任意包装函数或动态数组。
显式输入尺寸与最终有效的 X/Y 方向锁定都在成功创建时
写成约束，与几何一起提交和撤销。提交前解除锁定则不生成方向约束，切换轴只保留
最终方向，取消草稿不写入任何内容。原有每段提交后复位、轴锁与角度互斥的规则
继续成立；普通自动吸附不因此生成约束。点线支持固定点、重合、水平/垂直、
长度/角度、中点和点 X/Y 约束，圆支持半径约束，见 [草图方案](plans/sketch-editor.md)。

Select 模式下 Ctrl 点击切换选中、Shift 点击只追加，普通点击替换选择，空白点击或 Esc 清除。选中点后
提供固定与 X coordinate/Y coordinate（固定坐标值，不是移动方向），两个点可重合；
点与不以它为端点的本层线可施加中点关系，三个点也可按中心、两端点的顺序施加中点关系；单线或多线
提供水平/垂直及长度/角度，圆/圆弧提供半径，圆弧另有扫角。约束作用于源码图元，
不是修剪产生的临时分段；同一图元的多个分段去重，重叠高亮的本层图元一起处理。
已存在同类约束的目标不重复追加；数值输入复用绘制的验证、原生文本撤销及确认/取消。
约束统一为 `['kind', target, value?]`，尺寸值不与目标嵌套。已施加的工具按钮保持
启用并高亮，点击移除所选目标上的该类约束；多选只有部分已有时显示混合态，点击移除选集中
出现的该类约束。添加则要求完整选集满足前置条件。移除时保留可编辑几何的当前显示值，不返回旧求解初值；表达式仍保留。
一组约束在同一源码事务中追加，取消不写，源码撤销恢复整组；Fixed 同事务保存可编辑
点轴的当前显示坐标，表达式保持原文；表达式轴显示值与源码初值不同则禁用 Fixed 并提示
使用 X/Y，不将点跳回旧初值。上游不可修改，但具名上游点可参与本层点关系。
多选 Delete 仍按区间修剪，合并相邻存活区间，清理失去连接的孤点与相关约束，一次撤销。
线间平行支持两条或更多本层直线；相互垂直和夹角要求恰好两条，线间等长尚未接入。

单击图中约束标记（或聚焦后 Enter/Space）切回 Select 并选中相关点或整个源码曲线。
本层尺寸直接打开输入框并选中完整源码值，包含变量和复合表达式；确认只替换对应约束的值节点，
保留顺序、注释和其他约束，一次撤销。取消、选择或源码变化关闭未提交输入。
添加和编辑约束共用单个 TypeScript 表达式的语法验证；变量、函数调用等在正常编译中求值，
不在 UI `eval`，不把表达式替换成求值后的数字。只读上游约束可以选择，但不能改写。
所有尺寸输入按文字宽度从 82px 自适应到 320px，并受窄视口宽度约束；保留原生焦点及文本撤销。
绘制阶段的即时定位输入仍接受数字；几何坐标表达式仍遵守拖动时的 AST 只读规则。
工具栏的已施加按钮仍用于移除，画布标记用于选择/编辑。工具栏和标记共享同一套
Lucide 风格 SVG：坐标使用 X=/Y=，线方向和各尺寸使用独立的工程示意。

区域填色复用 core 的解析区域提取，不参与拾取，也不改变源码实体身份。开放草图正常
编辑；`.face()` / `.faces()` 才要求完整的有效闭合边界并提供建模诊断。
输出正常面模型，单面 `.extrude(distance)` 与函数式入口共享长度参数 schema；
多个面由用户 `map`，不增加特殊集合工具。参考 `examples/sketch-modeling.ts`。
工具改码后的全文格式化会暂时使旧源码位置失效，此时参数控件禁用，待新编译恢复
映射后启用；仍可映射的连续参数编辑保持可用，不按操作名称添加特例或延时。

`sketch.edit` 使用同一 ToolEngine 和原子源码事务。每段线创建、拖动释放或删除
是一次编辑；拖动预览不改源码，Escape 取消。没有独立 Point 创建工具，线工具
按需生成端点；点实体、点引用、拖动和删除仍保留。
工具栏使用剪刀图标的 Trim 模式：悬停高亮、直接点击修剪，不依赖预先选择，连续点击保持模式，
Esc 退出。可直接清除本层独立点；上游和不可源码编辑的几何不可修剪。
Select 模式仍可选中后按 Delete/Backspace。两条交互路径共用同一删除事务。
直线的选择、高亮和修剪按相邻顶点/交点间的小段工作，无内部边界时才删整线。
边界包含经过的已有点、有限直线/圆/圆弧交点与共线重叠端点，不随屏幕缩放或 Snap 开关变化。
解析求交共用模型精度，相切只产生一个边界，圆弧缺口不参与切割。
圆和圆弧可作为本层/只读上游的边界，修剪直线不修改它们的几何、表达式或约束。
显示和选择不改源码。删除后剩一段保留原线 ID，剩两段退役旧 ID、为两段分配新 ID。
同一本层区间的共线重叠段（包括反向定义）整组高亮，提示数量，一次点击或 Delete
全部修剪；不要求重复点击。分组使用模型几何容差，不使用鼠标命中的像素范围；
相交、端点相接、近邻平行线和只读上游不并入。同批每条原线独立遵循 ID 和约束规则，
新交点跨重叠线复用，按整批结果清理孤点。不做全局去重，也不删除点击区间外的部分。
本次删除后失去所有线连接的本层点与相关约束同时移除；已有边界点复用引用，
纯交点按需生成本层普通数值点。连接包括线的源码端点和几何上经过点的线（T 形连接），
不清空无关的独立点（例如未连接线的矩形中心），不删除共享点、上游点或其他线。
生成的交点不隐含关联约束；以后移动切割几何不会自动带动新端点。
原整线的长度约束移除，水平/垂直/角度约束保留并映射到剩余线，角度表达式不转成数值。
关系改写和几何是同一次事务/撤销；连续操作的临时快照约束顺序与源码一致。
上游可作边界但仍只读；无法生成具名引用或无法定位要拆分的方向约束目标时，
拒绝该次删除并显示提示，不猜测引用或覆盖表达式。
重叠批次也必须全部可安全改写，否则整批拒绝，不部分删除。
点删除仍会同时移除关联本层线与约束，并使用相同规则清理这些线留下的孤点；
所有内容同一次撤销恢复。
成功写回后立即显示本次结果，等待重编译期间继续编辑不会重复分配 ID；
源文件变化会取消未完成手势，旧快照不得覆盖新源码。返回编辑器焦点时沿用统一
格式化与光标映射，格式化和工具编辑保持同一撤销组。
草图与 3D 工具使用同一源码更新提示；反馈栈的实际高度驱动绘制输入区底部留白，
收起或展开差异、同时出现诊断时都不遮住连续绘制的输入框，也不主动抢焦点。

连续线使用独立于 DOM 的绘制会话。起点输入 X、Y，下一端点输入长度和角度
（度）；显式填写的字段在成功提交时生成相应约束。空字段跟随鼠标，已输入的量
优先于吸附，非法或未完成的数值不能提交。输入文本和最后有效预览值分开保存，
不因 pointermove 或重编译覆盖正在输入的文本与光标。当前只接受数字字面量；
表达式仍在源码中编辑，不在 GUI 中求值后替换成数字。

确定线起点后，X 键锁水平、Y 键锁垂直，按另一轴直接切换，按同一键解除。
锁定是通过起点的双向轴，鼠标决定正负方向，可配合长度输入；不是固定坐标值
或仅朝正方向的射线。轴锁和显式角度互斥，最后一次操作生效：锁轴清空角度，
编辑角度解除轴锁，长度保持不变。绘制画布与数字字段均支持该快捷键，忽略
按键重复、IME 合成及 Ctrl/Meta/Alt 组合，不截获普通编辑器的输入。
提示显示 X/Y locked；Snap 关闭或 Alt 绕过吸附不会解除显式方向锁。
只吸附到符合方向和长度的点，保留端点身份；提交本段或取消绘制后解除锁定，
重编译和草图内部焦点切换不解除。X 提交为 `horizontal`、Y 提交为 `vertical`；
提交前解除则不生成方向约束。

画布和数字字段共用焦点作用域：直接键入数字进入字段，Tab / Shift+Tab 循环
本次绘制字段，Enter 确定起点或提交下一段线。提交后复用终点 ID 作为下一段
起点，清空长度/角度输入；每段单独撤销。Escape 结束当前连线并丢弃未提交的
预览，保留已完成段，再次 Escape 退出工具。切换工具、离开草图或窗口失焦
取消草稿，进入草图内的输入框不会取消。
字段中的撤销/重做使用原生文本历史；画布中的撤销/重做仍交给源码历史。
绘制与求解预览按实体 ID 复用 SVG 节点，状态文字就地更新，不在每次输入中替换
文字或重写不可见的 placeholder。Chrome 在页面其他文字变更（如 Monaco 异步
着色）时仍可能划分新的原生撤销组；不以自建文本历史替代浏览器分组。

吸附结果携带点身份或临时坐标及原因，命中阈值采用屏幕像素。支持点、原点、
网格以及线段的水平/垂直方向；Snap 按钮开关，Alt 临时绕过。数字约束与目标
不兼容时不吸附，更不捏造端点引用。水平/垂直和网格只辅助本次创建，不持久化
为约束。网格使用 1/2/5 自适应细分，小格在各缩放级别约为 8–20 屏幕像素，
每五格加深主网格；吸附与显示使用同一间距。方向吸附也可将自由轴对齐到网格。
拖动明确吸附到已有点时，松手可将本层点写为目标的别名并保留原 ID；坐标相近不自动合并。
合并预览先删除因此退化的本层直线及其约束，再求解并复核保留曲线的连接；两点 ID 都保留。
预览保留未合并的数值续算状态，移开目标恢复点线；松手一次写回别名、参数与删除，取消不写。

运行时 snapshot 分层保留实体 ID 和点的所属层。编译器只为真实传入的内联数组
及其选项记录可编辑 sourceRef；编程生成的数组仍可显示。Resolver 只接受显式 tuple 和
数字 ID，移动只替换数字参数（点坐标、圆半径），不覆盖表达式；其他 tuple 和注释保持原文。
删除本层点同时移除引用它的本层几何及受影响约束，不改变只读上游，也不改写下游源码引用。

运行时与拖动共用数值求解入口；没有作者方程或推断连接的移动直接计算，不等待 Worker 中的重新编译。
规则调度器将完整数值上下文交给按序匹配的规则，不预先分类控制点/端点或拆分独立部分。
首批规则覆盖圆/圆弧/中心矩形的局部平移偏好、端点优先参考中心否则参考最远关联点，
以及唯一无约束交点按各支分别选择策略。角色识别和连通性计算都是规则内部细节；关联
沿实体引用和显式约束传递，不把坐标接近或几何相交当成共享身份。
点在曲线上规则识别手势开始时已在直线、圆或圆弧上的点，使用模型几何容差而非屏幕阈值。
点沿曲线可滑动，直线和圆弧限于实际区间，圆弧边界遵守 CW/CCW，不扩大为整圆。
端点、圆心和半径变化时保持在线关系；圆心优先平移随动点，半径变化优先保留极角。
随动点采用软姿态目标，不隐式固定；不自动拆曲线、创建交点或改变身份，途中经过其他曲线不产生连接。
即使没有显式约束，这些临时方程仍通过 Worker 求解；写回后的正向重放独立校验所有原有在线关系。
规则返回按优先级执行的软目标组，而非依次尝试的候选。通用执行器不判断几何角色；
圆弧端点手势先优化关联圆心的起始位置；圆心手势先优化所属圆/圆弧的起始半径，再求鼠标的最近可行位置。
共心曲线的半径一起参与；端点兼任圆心时按关联圆心保持 → 所属半径保持 → 鼠标目标排序。
半径偏好同样让位于硬约束，不写入持久 radius 约束。其他手势从鼠标目标开始。
半径自由时可随端点调整，受约束或表达式锁定时沿可行圆周移动，不为追随鼠标牺牲圆心偏好。
随后优化局部平移，最后令关联外围尽量少动。端点同时作为其他曲线圆心时也遵守该顺序，
平移初值与径向投影使用同一端点位置，避免附属曲线初值变形。
局部平移目标取手势起始点加实际可达圆心的位移，保留前面阶段已取得的半径；
连接外部线段不再导致圆心规则退出，外部点通过已有约束响应。
局部平移层在圆心可达位置确定后，用同一平移目标重新准备随动点初值，遵守此前取得的
坐标/半径锁，再求解完整约束；不直接将目标当结果，也不把所有软参考都改为新初值。
所有规则阶段之后统一软保持非拖拽点的手势起始位置，只收敛剩余自由度；已确定的平移
不被拉回。按 canonical 求解点生成目标，别名不重复加权；半径手势包含全部点，
不额外增加通用半径目标。末层没有剩余目标时不重复求解。
每层遵守全部硬约束，无法完全满足的软目标保留最佳可达结果，不跳过该层。
求解精度统一按局部图元尺度预算，原生迭代与十进制简化只占预算的一部分；清理后
独立验证硬约束和圆弧结构。阶段间区分作者/目标值与求解中间种子，后者不被当成应恢复的
准确输入。已满足精度的圆弧数据不重复投影，预览与正向编译不引入新一轮尾差。
后层临时保留前层已经求得的目标参数；保留选中的解，不保留所有等价最优解空间。
这不是预先将原位置变成硬锚；所有阶段锁每帧重建，后续手势仍可自由拖动。
临时目标不写入源码，也不改变 snapshot 的持久自由度。普通运行时不运行拖动规则。
拖拽时 AST 按坐标判断可编辑性，表达式轴临时锁在本次源码的求值，
而不是已经移动过的显示解；例如 `[width, 0]` 锁 X、可拖 Y。锁覆盖所有本层点，
预览、UI 与源码事务共用逐参数掩码，圆半径使用同一通道；数值求解器不接触 AST。表达式锁改变显示几何
时先满足这些锁与原有约束，再建立规则参考几何。永久约束不放宽。
连续请求以上一帧为数值起点，参考几何保持手势开始时的值；鼠标移动合并请求，松手等待最后一次求解。可回写点
保存求解后的绝对坐标（含原始数据与显示位置不同的锚点），不向旧初值叠加位移；
未移动时不规范化作者数据。预览对即将无损写入的实际数据执行与编译相同的正向求解，不二次舍入。
表达式源码完全保留，只回写同一点中可编辑的轴。初始数据不满足约束且普通求解
已移动表达式轴时，首次施加作者值锁可能调整显示位置；这与松手后的重放一致性
不同。验收与剩余问题见 #23。取消、源码变更或切换草图使旧响应失效。
冲突与失败不应用坐标。每次原生求解独立检查全部硬约束残差并释放对象；
冲突的临时约束索引映射回源码 tuple，冗余索引和自由度保留在 snapshot 中。
持久约束默认显示图标及数值标记，Constraints 可整体开关。悬停或键盘聚焦标记
高亮实际关联点/曲线；中点关系画中心到两端的虚线，重合关系连接两点，半径连接圆心与实际圆周。
标记只读、区分本层与上游，位置随当前求解预览和缩放变化；不显示临时拖动锚点，
也不向源码增加约束 ID、布局坐标或虚构关系。标记本身不触发底下几何的绘制/拖动/修剪。

对角点矩形与直线共用绘制会话契约、数字输入、吸附及草稿 ID 分配。选首角再选对角，
宽高均为正尺寸、鼠标决定所在象限；未输入的轴继续跟随鼠标。生成普通四点四线和
四条水平/垂直约束，首角显式 X/Y 和宽高在同一事务中写约束；宽高分别约束相邻一边，
不重复约束对边。实际吸附的首角/对角复用引用，两个中间角创建本层点。零宽高拒绝提交。
整个矩形一次撤销；成功后留在 Rectangle 等待下一首角，Esc 取消草稿后再按退出。
绘制预览由工具给出原生曲线集合，UI 保持 SVG 节点，数字字段仍保留浏览器原生文本撤销。

中心矩形复用相同实现：先选中心再选角点，Width/Height 始终是完整边长。
中心为可引用的普通点，在新角点之前分配 ID；中心及所选角点可复用本层/具名上游引用。
四条水平/垂直约束加一条 `['midpoint', [center, oppositeA, oppositeB]]` 关联四角，
不额外生成对角线或第二组冗余中心约束。中点约束属于通用三点关系，不依赖矩形。
创建、源码序列化、删除相关点时的约束清理及撤销均使用现有事务。
规则从四条轴向边与中点约束识别中心矩形；拖中心优先整体平移，硬约束或坐标锁要求时可变形。
拖角点可软参考中心，但中心必要时让位于永久约束和鼠标可达位置。
可交互几何的普通、选中、约束关联和修剪预览线宽统一为 2px。

圆工具先选圆心（可输入 X/Y），再输入半径或点圆周。输出普通圆心点及
`['circle', id, [centerPoint, radius]]`；圆心吸附复用本层/具名上游点，不创建圆周点。
显式 Radius 生成 `['radius', circleId, value]`，未输入半径保持自由；整个圆一次提交/撤销。
Select 中拖圆周调整半径、拖圆心调整坐标；表达式半径只读，尺寸约束继续生效。
原生圆几何、参数输入、约束标记与源码回写共用现有通道。取消草稿不留几何或约束。
删除圆清理本次失去连接的本层圆心及相关约束，其他圆/线共享的圆心与上游点保留。

圆弧使用 `['arc', id, [centerPoint, radius, startPoint, endPoint, 'cw' | 'ccw']]`，
半径是普通当前数据，不隐含尺寸约束。初值不一致时，各弧按圆心/半径和端点方向
同时生成径向投影建议，共享端点取平均；已锁定、上游、fixed 或显式 X/Y 轴不覆盖。
已有显式 radius 约束时用该尺寸初始化半径，否则使用普通半径数据；不先投影到
矛盾半径再回缩，避免欠约束圆弧因此发生整体漂移。已锁定半径不覆盖，冲突仍报错。
该过程只建立求解起点，随后结构方程与显式约束统一求解，半径仍可变化，不增加锁或软目标。
`radius` 约束可指向圆弧。Arc 按中心（X/Y）、起点（Radius）、终点（Sweep）三步绘制；
新绘制默认顺时针 CW，R 切换方向；取消或完成后恢复 CW，已有显式方向不变。
终点投影到起点半径，零半径/重合端点拒绝。
绘制、取消、源码回写和单次撤销共用现有路径，引用继续保留具名上游身份。
端点与中心按普通点拖动，也可拖弧边调整字面量半径。ArcRules 保持端点位于该圆上，
半径共用 AST 可编辑性、表达式手势锁、参数回写及舍入源码重放。
显式 Sweep 写入 `['sweep', arcId, degrees]`，严格大于 0° 且小于 360°；
空白扫角由鼠标确定，不生成约束。R 改变 cw/ccw，不改变输入的扫角幅度；
数值校验、Tab、取消及源码撤销沿用同一绘制输入实现。原生角度按方向展开，
同时检查实际有限弧的扫角残差；扫角标记显示角度并连接真实圆心和两端。
解析曲线共用显示、有限区间拾取、缩放边界、半径标记和孤点连接判断；
圆弧半径是可回写几何标量，扫角不另存初值；尺寸锁定只在独立约束中，不把缺口当作连接。
圆与圆弧按同一解析交点/已有点划分区间；Select + Delete 与 Trim 悬停/直接点击
使用相同事务。圆按循环区间处理，不制造零角接缝；零或一个分界点时删除整圆。
圆剪去一段后转为 CW 圆弧并保留 ID；有限弧剩一段保留 ID，剩两段退役旧 ID，
保留其 cw/ccw。圆心和半径表达式原样保留，radius 约束映射到存活弧，整弧 sweep 移除。
重合圆/弧按同一区间一起高亮和删除，共享新交点、清理新孤点；一次源码编辑/撤销。
上游只提供边界，不能被修剪；新端点是普通数据，不隐含关联约束。Select 的半径拖动不变。

派生点引用使用词法作用域内已确认值身份的具名草图，例如 `base.point(2)`。
当前 receiver 名可由实际运行值确认；重赋值、遮蔽或多次执行使其他 binding
身份不明确时，不猜测引用。不存在安全名字的上游仍可显示，但不能作为新线的
引用端点；不为匿名上游增加相对层级语法或自动改写变量声明。

## Scope 与歧义

工具必须把以下信息作为解析上下文，而不是自行决定：

- 当前选中的是模型、occurrence、组件、face、edge 还是 operation。
- 一个参数会影响哪些 occurrence。
- 修改已有共享参数，还是为当前 occurrence 构造新表达式。
- 当前 topology 是否还能稳定映射到产生它的源码。

例如拖动由 `x * postOffset` 定位的单个支柱时，resolver 可以提供：

- 修改 `postOffset`，影响两个支柱。
- 为当前 occurrence 构造局部表达式。

第一种可以直接 preview；第二种属于结构编辑，应在提交前展示将要生成的源码。

对于没有自身位置关系的布尔结果，工具不会猜测应修改哪个输入实体，也不会为结果发明绝对位置；
只有用户选定具体关系 scope 后才生成对应的 edit plan。

## 方向 bound 与关系旋转

`on` 的左右书写角色与 `relate` owner 分别记录。源码预览、位置工具和顶点选择
以 owner 确定接受摆放的 self；反向书写时，offset 的源码数值仍按目标参考系定义，
预览位移使用对应的反号。Bound 装饰直接使用运行时返回的有限矩形和 facing，
不从网格猜测一个同向的真实拓扑面。关系快照同时给出自动匹配的源边界和完整
sourceBounds；`on` 源侧画用于接触计算的完整包围盒，拓扑源只包含选中元素的范围。
辅助面使用与包围盒角线同色的半透明填充；已有包围盒时复用其轮廓，不叠加角框，
没有包围盒时才自行补角框。

关系预览区分当前关注侧、当前 constraint 的另一侧和其他相关对象。两侧标记统一
颜色与屏幕尺寸，关注侧保留各标记基础不透明度，另一侧整组乘 0.7，其他对象灰暗。
`on`/`align` 及后续链调用关注 owner self；`on`/`align` 的目标括号内关注实际目标引用，
因此反向书写 `base.on(self.up).offset(...)` 时，两处都关注 self。模型本体使用
不透明度上限而非乘数：主侧面 0.82、线点 1，
次侧 0.7，其他对象面 0.18、线点 0.28。材质本身更透明时保留它的不透明度。
App 与网站配图入口共用 source decoration providers，保证关系和原点标记一致。

完整 Constraint 和未完成的 pivot/axis 选择共用 ConstraintExpression trace。
中间值保留原关系、self 和参数来源，但 relate 只接受完成的 Constraint。
每次调用有独立源码位置与阶段预览：`on`/`align` 显示基础关系，`offset`/`rotate`
显示执行到当前调用的结果，尚未完成的支点和轴选择沿用前一姿态；后续调用不影响
当前几何或工具。已继承的关系仍有效，当前 callback 的其他 constraint 在完整
`relate` 结果上联合求解。中间阶段的错误只属于该预览，不使有效最终模型失效。
pivot 显示支点与三个平移轴；pivotVertex 使用 self 的
顶点命名空间；around 显示解析后的轴；rotate 显示三个 Euler 环或一个轴环。
旋转操作元数据以最终 self 的局部坐标表达当前调用的参考系，保留实际组合位置，
因此反向关系、非零 XYZ 角度及外部轴均可生成与重算一致的预览。

位置和旋转工具使用现有参数来源、安全上游参数判断、表达式增量、预览/取消、
源码写回与撤销流程。Bound 翻面只改变 facing；offset 与编辑坐标系保持不变。
