name: Build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run lint-prettier

      - name: Build packages
        run: npm run build:packages

      - name: Build app and render-only entry
        run: npm run build --workspace @code3d/app

      - name: Install render browser
        run: npx playwright-core install --with-deps chromium

      - name: Generate website model image
        run: npm run render:web-image

      - name: Build website
        run: npm run build --workspace @code3d/web

      - name: Upload website artifact
        uses: actions/upload-artifact@v4
        with:
          name: code3d-www
          path: packages/web/dist/www
          if-no-files-found: error
