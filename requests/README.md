# code3d request inbox

Each request has one file. Open requests live in this directory; completed or
otherwise closed requests move to `closed/`. Moving a file changes its status,
so request files do not carry a separate status field.

Request IDs are stable and are not reused.
