# Code3D repository instructions

所有会保留的开发遵循[持续重构原则](.agents/skills/code3d-prototyping/SKILL.md)，
工作区与交付遵循[隔离开发流程](.agents/skills/worktree-development/SKILL.md)。

## 默认响应式迁移

每次功能新增、修改或修复，都主动读取并应用
[响应式技能](.agents/skills/code3d-reactivity/SKILL.md)。在当前任务中默认完成相关
scope 的响应式重构，无需用户另外提出“迁移到 MobX”或“顺便重构”。

scope 沿本次功能的状态所有者、派生值、写入入口、消费者和生命周期确定，覆盖
实际联动所需的依赖闭包。同步迁移调用方、验证和文档，删除被替代的手动刷新与
重复状态路径。无关功能保持在任务范围之外；纯样式、文案和独立计算按技能的
适用边界处理，保留模型值语义及跨进程边界。

实际开发证据改变已有判断时，在同一任务中修订、合并或删除技能中的对应规则，
并同步受影响的实现。具体范式与自进化规则以技能为唯一出处。涉及项目结构调整
时，仍须在实施前与用户讨论理由、方案和影响。
