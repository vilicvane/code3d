# code3d 设计目标

## Goals

- 使用完整的 JavaScript/TypeScript 自由构建和组合模型。
- `cached(fn, {encoder, decoder}?)` 为同步确定性计算提供共享缓存；内存保留结果，
  编解码仅用于磁盘。`definePrimitive()` 默认缓存构建与几何分析，每次仍产生独立模型
  和追踪信息。Core 内部计算与 screws 共用相同机制，不各设 LRU。引擎为静态定义及
  依赖生成内容身份，动态闭包状态须显式传参；未经引擎识别的函数按对象身份复用内存。
  见 [#106](https://github.com/vilicvane/code3d/issues/106)。
- 正常取消构建保留已完成的内核操作缓存，与抛错采用相同的工作集/LRU 收尾；
  下一版重新执行源码，复用相同计算，不展示取消结果。无法响应的构建仍可强制终止。
- 历史建模缓存按内存预算管理，默认 2 GiB，不按固定条数截断编辑历史。
  内核已分配块与缓存 JavaScript 数据估算共同参与预算，超额按 LRU 淘汰历史；
  当前工作集始终完整保留，所以预算不是页面总内存的硬上限，也不提前预分配。
- 浏览器中的纯内核结果另存为 OPFS 持久缓存，刷新页面、重建 Worker 和重新打开
  项目后按需复用。源码与追踪每次重新求值；内存和磁盘历史分别执行 LRU。
  存储损坏、不可用或配额不足不妨碍建模。实现内容变化隔离命名空间，不迁移旧格式。
- code3d 模型项目是普通的 Node/TypeScript 项目，自行管理 `package.json`、
  lockfile 和 `node_modules`。Browser storage 由 App 在浏览器内安装 npm 包，
  每个子目录清单使用平级 `code3d-lock.json` 与 `node_modules`，普通运行保持锁定版本；
  文件树右键 Install package 在所选目录添加依赖；缺失清单时一并声明 core 为 latest，已有清单
  保留其他依赖。在 node_modules 内操作时向上查找项目清单，不修改依赖包的文件。
  项目清单右键 Update dependencies 按现有版本约束忽略旧锁重新解析，成功后更新安装与锁。
  本地文件夹仍由用户自己的包管理器安装。TS、运行时和资源共用安装文件与真实路径，
  包声明及原始源码按需只读打开。见 [#90](https://github.com/vilicvane/code3d/issues/90)。
  App 在模型所在清单及其祖先未声明 `@code3d/core` 时提供完整内置
  core/screws/materials，支持零安装开始建模；声明 core 后全部由项目自己的依赖接管。
- 同一份项目源码遵循标准 Node ESM/TypeScript 规则，既可由 App 加载，也可
  在受支持的 Node 环境中直接执行。
- 关注模型对象本身，而不是它的构建过程。
- 模型可以导出、导入、复用、实例化和渲染。
- 当前编辑的任意源码文件都可以直接作为执行根并预览其中的模型。
- 代码与 GUI 围绕同一个模型对象交互。
- 编辑模型时以代码为主；GUI 只在代码不便操作时提供重要但克制的补充。
- 展示模型信息时充分使用 GUI，不要求所有信息都回到代码界面表达。
- 交互具有明确的 scope：整体模型、实例或组件、局部元素或操作。
- GUI 补充代码中不方便、不直观的操作。
- 源文件是模型持久化参数的唯一来源；GUI 修改直接写回源码。
- 草图同样以源码持久化，但点线编辑以可视化为主：`sketch([['point', 1, [x, y]], ...])`。
  每层显式实体 ID 独立；派生保留只读上游，通过 `sketch1.point(id)` 引用上游点。
  点也可用 `['point', 11, 1]` 或具名上游引用表示别名：保留 ID，共用最终目标的坐标与
  求解变量，允许引用链，拒绝循环和缺失。拖动吸附到已有点在松手时原子写入别名；
  合并前删除因此退化的本层直线及引用这些线的约束，两点 ID 均保留。吸附预览包含合并与删线，
  移开或取消恢复原拓扑；松手将所有变化作为一次源码事务。其他曲线的拖动随动及连接继续复核。
  表达式不可被替换，固定点冲突拒绝合并。坐标相近不会自动改变身份。
  求解尾差按局部图元尺度清理，优先准确输入/拖动值，独立复核硬约束及圆弧结构；
  原生收敛、尾差清理和几何精度共用数值预算。分层的中间解只是迭代种子，不晋升为
  作者准确值；已满足精度的圆弧数据不反复投影，防止正向重编译重新引入尾差。
  写回不二次舍入，预览与源码重新求值采用相同数据。
  草图不持久化 `nextId`，新建实体取当前本层最大 ID 加一，不重排已有 ID。
  草图画布铺满右侧 viewport，工具浮于画布上；viewport 错误仅属于当前草图或其上游，
  无关模型、其他草图和失败的下游派生不影响当前草图的错误提示。源码编辑器仍展示全部诊断。
  尺寸约束输入保留作者表达式，由正常编译在原词法作用域求值，不在 UI 求值后替换为常量。
  成功求解但几何初值与显示结果超出局部几何容差时，以警告提示不同步，不自动修改源码。
  用户可显式同步可写几何；表达式、只读上游及同段源码的其他运行实例不被隐式改写。
  直线按经过的点和交点分段选择，只在删除时改写源码：剩一段保留旧线 ID，
  剩两段则退役旧 ID、分配两个新 ID；其余实体不重排。
  同一本层几何区间的重叠直线一起高亮、修剪和撤销，包含反向重叠；不删除区间外部分或只读上游。
  拖动开始时已在直线、圆或圆弧上的点保留在线关系，可沿曲线滑动；直线和圆弧限定在
  实际有限区间，圆弧遵守 CW/CCW 方向，不滑到补全圆上。移动端点、圆心或半径时随动，
  圆心优先平移自身曲线及附着点，外围由约束响应；保留曲线类型与 ID。按模型几何容差识别，
  不将途中经过的曲线视作连接，不隐式拆曲线、创建交点或合并点身份。
  拖动圆弧任一端点时，先在硬约束下尽量保持关联圆心，再优化端点对鼠标的跟随；
  半径自由时可调整，受限时端点沿可行圆周移动。这一手势偏好不生成持久固定约束。
  拖动圆或圆弧的圆心时，先尽量保持该圆心所属各曲线的半径，再优化鼠标跟随与局部平移；
  端点兼任圆心时，关联圆心保持先于所属半径保持。各层仍遵守硬约束，不新增持久半径约束。
  关系只作为本次拖动的临时求解条件，遵守已有约束、表达式锁和上游只读；写回实际坐标，
  正向重编译复核连接，无新增持久约束。
  `face()` 要求唯一闭合区域（含孔），`faces()` 返回所有区域的普通只读数组；未来 ID
  选择不改变无参语义，不把数组位置当稳定 ID。提取包含上游，独立轮廓分开，嵌套按
  实体/孔/岛交替；开放、分叉、相交和相切边界诊断，不隐式修剪或改写实体。
  草图 `[x,y]` 映射为模型 `[x,0,-y]`，不居中；单面 `.extrude(distance)` 与函数式
  `extrude(face, distance)` 等价，有限非零有符号距离沿平面法向。多面由用户 `map`，
  不提供增强集合或批量拉伸重载。`stock.cut(tools)` 与 `cut(stock, tools)` 等价。
  loft 的截面数组属于单次运算，支持每截面无孔或一个对应孔；孔数不符或多孔缺少
  对应关系时诊断，不默默填孔，不按遍历序号猜测匹配。
  删除时一并移除因此失去所有线连接的本层点与相关约束，不清空无关独立点或只读上游点。
  几何当前数据与独立的 `constraints` 分开；约束不持久化 ID。
  约束 tuple 为 `['kind', target, value?]`，如 `['length', 3, 20]`；多点 target 仍为数组。
  线间关系为 `['parallel' | 'perpendicular', [line1, line2]]`；两线夹角为
  `['angle', [line1, line2], degrees]`，从各线作者起终点方向计算第一条到第二条的
  有符号角度（正为逆时针，模360等价），不要求相交、不自动折叠为锐角。
  单线 `['angle', line, degrees]` 仍为相对+X的方向角，GUI使用独立名称 Orientation。
  Trim按存活片段继承方向关系和角度表达式，删除任一参与线则清理关系。
  显式输入尺寸和最终有效的 X/Y 方向锁定在创建时生成约束，普通自动吸附不生成约束。
  拖动规则返回分层软目标：先求鼠标可达位置，再局部平移，最后外围少动；所有层遵守硬约束。
  规则之后统一软保持所有非拖拽点的手势起始位置，只收敛剩余自由度，不拉回前层已确定的平移。
  后层仅在当前帧保留前层已求得的目标参数，不预先锁死原位置，不持久化阶段锁或改变模型自由度。
- 模型参数可以追溯到可编辑的上游源码。
- 模型对象采用语义不可变的 value semantics；建模操作产生新值，不改变已有值的可观察模型语义。
- `@code3d/core/three` 重导出 Core 依赖中的原生 Three.js 类和类型，主模型及复用包通过该入口共享实例。`.material(threeMaterial)` 原子替换整个材质，调用时捕获原生 JSON 和已加载纹理像素；之后修改输入实例不改变旧模型。组合体覆盖整棵子树，外层优先。Core 快照跨 worker 传值，App 还原并释放材质/纹理，建模强调使用独立预览副本，Render 与 PNG 使用作者材质。CSS 颜色简写整体选择默认材质。见 [#77](https://github.com/vilicvane/code3d/issues/77)。
- 模型可以公开类型安全的具名点、线、面参考元素，供复用方直接选择、约束和可视化。
- 几何模型经 `expose` 成为相应的拓扑引用，已有拓扑引用保留身份；纯参考几何保留 Anchor 语义。拓扑引用支持按维度继续查询，其几何来源与装配归属分别记录，约束作用于外层模型。见 [#27](https://github.com/vilicvane/code3d/issues/27)。
- 拓扑 ID 按顶点、边、面分别独立：当前操作新增元素使用从 1 开始的数字，一一对应的继承元素使用 `[输入序号, ...原 ID 路径]`；分裂或合并后归属不唯一的元素不继承旧身份。多输入操作保留所有输入的来源，内部计算步骤不进入路径；不改变拓扑的变换与引用保持完整 ID。见 [#39](https://github.com/vilicvane/code3d/issues/39)。

- `on` 只允许平移，左侧为模型或所选有限点/线/面，右侧仅为 up/down/left/right/front/back 方向 bound。源极值边界在目标方向下计算，不自动转动、居中或匹配真实拓扑面。见 [#38](https://github.com/vilicvane/code3d/issues/38)。
- `align` 独立表达底层几何重合或包含关系，可求解位置和姿态；忽略曲线/曲面裁剪边界和参数起点，保留未约束自由度。线以 `reverse()`、面以 `flip()` 选择方向，不改变参考轴。`align.offset` 在目标参考轴中对 self 做对齐后的平移，再执行旋转链；零偏移不额外定位。见 [#43](https://github.com/vilicvane/code3d/issues/43)。
- `relate` 确定接受摆放的 self，与 on 的书写方向分离；原 receiver 引用可重绑定。旋转用独立的 pivot/pivotVertex/around 与 rotate 链显式表达，局部坐标和顶点归属以 self 为准。见 [#36](https://github.com/vilicvane/code3d/issues/36)。

- group 默认原点在构造时取已求解的直接成员原点包围盒中心，轴向不变；嵌套 group 仅贡献自身原点。所有模型支持 originPoint(pointRef) 与 originOffset，组合体原点操作统一重表达已装配结果并保留内部约束；重复来源须选择具体实例。见 [#54](https://github.com/vilicvane/code3d/issues/54)。
- 模型原点在自身局部坐标恒为零；originOffset(d) 将内部点坐标重表达为 p-d，旧模型值保持不变。位置用数组，尺寸、位移增量和分轴角度用独立数值。模型 XYZ 与几何参考架分开；默认 rotate/scaled 围绕当前局部零点。见 [#48](https://github.com/vilicvane/code3d/issues/48)。

## Non-goals

- 不把构建步骤、历史记录或特征树作为核心抽象。
- 不为方便 GUI 同步而限制 JavaScript/TypeScript 的表达能力。
- 不用虚构模块或声明副本代替真实包产物。项目已声明 core 时，不用内置包掩盖
  缺失或不兼容的依赖；内置模式与项目模式各自保持一致的类型和运行时实例。
- 不要求 GUI 能反向编辑任意 JavaScript/TypeScript 代码。
- 不用 GUI 取代代码，也不复刻完整的传统 CAD 工作流。
- 不为维持一套平行的 GUI 编辑状态投入复杂的双向同步机制。
- 不让公开模型暴露 OpenCascade 句柄或直接操纵 Three.js 场景对象。
  `material()` 以原生 Three.js 材质为值输入，由渲染层管理还原后的实例。
- 不把 OpenCascade 的瞬时 hash、容器位置或遍历下标当作公开拓扑 ID。
- 不默认引入长度、角度等强数值类型；unit 只作为 UI 元数据。
- 不把全局绝对坐标作为对象组合和装配的默认抽象。

模型快照查询通过按几何体归并的 Worker 池并行执行，作者代码和动态关系求解继续
同步运行。主 Worker 统一管理内存 LRU、持久缓存、任务去重和取消，子 Worker
仅拥有当前输入的独立原生几何；全池共用默认 2 GiB 软缓存预算。单 Worker 与并行
网格使用相同二进制输入，保持网格和拓扑编号的一致性。见 [#80](https://github.com/vilicvane/code3d/issues/80)。
